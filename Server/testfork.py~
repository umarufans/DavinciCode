import os

print 'Process (%s) start...' % os.getpid()
print 'start'
pid = os.fork()# 从当前进程开启了分支，

print 'hello,world'

if pid==0:
    print 'I am child process (%s) and my parent is %s.' % (os.getpid(), os.getppid())
else:
    print 'I (%s) just created a child process (%s).' % (os.getpid(), pid)

